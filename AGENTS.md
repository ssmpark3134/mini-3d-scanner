\# 카메라 3대 간이 3D 스캐너 — Codex 작업 지침



\## 목표



\- PROJECT\_PLAN.md를 먼저 읽고 현재 구현 상태를 확인한다.

\- 2시간 미니 프로젝트로 정지 물체의 실루엣 기반 근사 3D 복원을 구현한다.

\- 최소 목표는 카메라 3대 입력, 실루엣 추출, 원본·마스크·설정 저장이다.

\- 전체 목표는 실제 마스크의 Visual Hull 근사 복셀 생성과 3D 표시이다.

\- 현재 초기 제공물은 Markdown 문서뿐이다. 계획된 스크립트가 이미 존재한다고 가정하지 않는다.



\## 기술과 구현 원칙



\- Python, OpenCV, NumPy, Matplotlib을 기본으로 사용한다.

\- 설명과 주요 주석은 한국어로, 함수·변수 이름은 의미가 분명한 영어로 작성한다.

\- 작은 함수로 나누고 불필요한 클래스·프레임워크·구조 변경을 피한다.

\- 장치 번호, 임계값, 영상 크기, 투영 설정, 복셀 해상도는 수정 가능한 설정으로 둔다.

\- 정상 동작하는 기존 기능을 유지하면서 필요한 부분을 수정한다.

\- 추가 라이브러리가 필요하면 사용 이유를 설명한다.



\## 작업 순서



1\. 환경과 카메라 장치 확인.

2\. 세 영상 동시 미리보기와 종료 처리.

3\. 촬영 공간·카메라별 초점 확인 및 가능한 경우 고정·수동 투영 기준 설정.

4\. 실루엣 생성·잡음 정리·미리보기.

5\. S 키로 원본·마스크·설정 저장.

6\. 합성 입력 투영 검증 후 실제 마스크로 복셀 교집합 계산.

7\. 3D 표시·결과 저장·재실행.

8\. 실행 방법·완료 상태·한계 기록.



각 단계의 성공 기준을 검증한 뒤 다음 단계로 진행한다. 사용자가 특정 단계만 요청하면 그 범위를 따른다. 하드웨어 확인이 불가능하면 가능한 파일 기반 검증을 수행하고 미확인 항목을 명시한다.



\## 카메라와 데이터



\- CAM1은 왼쪽, CAM2는 정면, CAM3은 오른쪽이다. 장치 번호와 논리 이름을 구분한다.

\- 각도 -45/0/+45도는 시작값이며 실제 배치에 맞게 수정한다.

\- 실제 물체 위치에서 카메라 3대 각각의 초점·경계 선명도를 확인한다. 세 카메라에 같은 초점 숫자를 강제하지 않는다.

\- 초점 고정은 장치가 지원할 때 적용한다. 지원하지 않는 설정은 성공으로 보고하지 않고 사용자가 확인할 방법을 안내한다. 고정 초점 카메라는 선명한 거리로 배치한다.

\- 초점 방식, 읽을 수 있는 설정값, 선명도 확인 여부를 촬영 설정에 기록한다. 초점·거리·줌 변경 후에는 수동 투영 기준을 다시 확인한다.

\- 프레임 읽기 실패, 빈 실루엣, 파일 저장 실패를 처리한다.

\- 종료·예외 발생 시 모든 카메라를 해제하고 창을 닫는다.

\- 정지 물체만 대상으로 하며 일반 USB 촬영을 정밀 동기화라고 설명하지 않는다.

\- 촬영마다 별도 폴더를 사용하고 기존 원본·설정을 덮어쓰지 않는다.



\## 복원 규칙



\- PROJECT\_PLAN.md의 좌표 약속과 수동 직교투영 근사를 따른다.

\- 좌우 대각선 마스크를 앞·옆·위 마스크처럼 취급하지 않는다.

\- 원본 크기·좌표를 유지한다. ROI 또는 축소 적용 시 투영 중심·배율도 일치시킨다.

\- 카메라마다 실루엣 폭·높이를 임의로 같은 크기로 늘리지 않는다.

\- 영상 범위를 먼저 검사한 뒤 mask\[v, u]로 조회한다.

\- 세 카메라 조건을 모두 만족하는 복셀만 남긴다. 시작 해상도는 40×40×40이다.

\- 수동 근사 결과를 보정된 정밀 복원이나 실제 치수 측정으로 표현하지 않는다.

\- 초점 조절과 체커보드 보정을 구분한다. 현재 120분 범위에서는 체커보드 보정을 후속 확장으로 두며, 사용자가 보정 구현을 요청하면 계획과 구현 범위를 갱신한다.

\- 보정 확장 시에는 카메라별 내부 특성·왜곡과 공통 좌표계에서의 카메라 위치·방향을 모두 다룬다.



\## 검증과 실행



\- PROJECT\_PLAN.md의 실행 명령과 인자에 맞춰 스크립트를 구현한다.

\- 장치 번호 대응, 동시 입력, 세 카메라의 초점 안정성·선명도, 마스크 품질, 저장 파일 재열기를 확인한다.

\- 합성 투영, 빈 마스크, 영상 경계, 카메라 조건 추가 시 복셀 수 비증가를 검증한다.

\- 합성 데이터 통과와 실제 카메라 검증을 분리해서 보고한다.

\- 같은 저장 입력·설정에서 복셀 배열이 재현되는지 확인한다.

\- 코드를 변경한 경우 관련 검증을 실행하고 실행하지 못한 항목은 이유를 적는다.



\## 완료 보고



\- 변경 내용, 실행 명령, 확인한 결과, 다음 작업을 간단히 한국어로 설명한다.

\- 완료 항목만 PROJECT\_PLAN.md의 체크리스트에 표시한다.

\- 실제로 실행하지 않은 코드나 접근하지 못한 카메라를 검증 완료라고 말하지 않는다.

\- 원본 데이터와 사용자가 작성한 파일을 보존한다. sources/가 있으면 읽기 전용 참고자료로 취급한다.



