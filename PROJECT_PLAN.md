# 카메라 3대 + OpenCV 간이 3D 스캐너 진행 계획

## 1. 목표와 범위

정지한 물체를 세 방향에서 촬영하고, 물체 실루엣의 교집합으로 대략적인 3D 형태를 만든다. 총 작업 시간은 **120분**이며, Python과 카메라 드라이버가 준비되어 있다는 전제의 목표 일정이다. 설치나 장치 문제에 따라 완료 범위를 조정한다.

- **실루엣**: 영상에서 물체 영역만 흰색으로 표시한 이진 마스크.
- **복셀(voxel)**: 3D 공간을 채우는 작은 정육면체 칸.
- **Visual Hull**: 각 시점의 실루엣을 공간으로 확장한 뒤 공통으로 남는 부피. 실루엣에 나타나지 않는 내부나 움푹 파인 면은 복원할 수 없다.
- **이번 구현**: 카메라 방향·영상 중심·크기를 수동 설정한 직교투영 근사 데모. 정밀 스캐너나 실제 치수 측정기가 아니다.
- **후속 확장**: 카메라 내부 파라미터, 렌즈 왜곡, 공통 좌표계의 카메라 자세를 보정한 원근투영 기반 복원.

최소 결과물은 세 카메라 원본·마스크 저장이다. 전체 목표는 저장한 실제 마스크에서 40×40×40 복셀을 깎아 3D 결과까지 표시하는 것이다. 합성 마스크만으로 성공하면 알고리즘 검증 완료로 기록하고 실제 스캔 완료와 구분한다.

## 2. 준비물

- 컴퓨터, USB 카메라 3대, 고정대, 연결 케이블.
- 흰 종이 또는 우드락, 무광의 진한 상자나 원기둥, 고른 조명.
- 바닥 위치 표시용 테이프와 각도·높이 확인용 자.
- Python, `opencv-python`, `numpy`, `matplotlib`.

첫 물체는 손잡이·투명 부분·반사가 없는 단순한 모양으로 고른다. 세 카메라 모두 물체 뒤에 흰 배경이 보이게 만든다.

## 3. 120분 시간표

| 단계 | 시간 | 목표 | 성공 기준 |
|---|---|---|---|
| 1 | 0~10분 | 환경·장치 확인 | 라이브러리 불러오기와 카메라별 프레임 읽기 성공 |
| 2 | 10~25분 | 3개 영상 미리보기 | 서로 다른 실제 카메라 영상이 한 창에 표시됨 |
| 3 | 25~40분 | 배경·배치·초점 설정 | 세 화면의 물체 경계가 선명하고 위치·초점 상태·영상 기준값 기록 |
| 4 | 40~60분 | 실루엣 추출·정리 | 세 화면 모두 배경 0, 물체 255의 안정된 마스크 |
| 5 | 60~70분 | 한 묶음 촬영 저장 | 원본 3장·마스크 3장·설정이 같은 폴더에 저장됨 |
| 6 | 70~95분 | 투영·복셀 깎기 | 합성 입력 검증 후 실제 마스크 교집합 계산 |
| 7 | 95~110분 | 3D 표시·재실행 | 저장 입력으로 같은 결과를 다시 표시 가능 |
| 8 | 110~120분 | 시연·기록 | 결과 이미지, 실행법, 한계, 완료 항목 기록 |

## 4. 단계별 작업 방법

### 1단계 — 환경과 카메라 확인

1. 아래 폴더 구조를 기준으로 구현 파일을 만든다.
2. 가상환경을 만들고 필요한 패키지를 설치한다.
3. 카메라 번호를 하나씩 열어 `isOpened()`와 `read()`의 성공 여부를 확인한다.
4. 번호와 실제 카메라의 대응을 기록한다. 번호 0·1·2는 예시이며 내장 웹캠이 포함될 수 있다.

**성공 기준:** 각 장치에서 정상 프레임을 읽고 종료 시 카메라를 해제한다.

### 2단계 — 세 영상 한 화면에 표시

1. 확인한 장치 번호 세 개를 함께 연다.
2. 처음에는 카메라별 640×480 입력을 요청하되 실제 받은 크기를 확인한다.
3. 미리보기만 같은 크기로 축소해 가로로 연결한다. 화면에 CAM1/2/3과 장치 번호를 적는다.
4. `Q` 또는 Esc로 종료하고 `release()`와 `destroyAllWindows()`를 호출한다.

**성공 기준:** 손으로 렌즈를 하나씩 가렸을 때 해당 화면만 변하며, 약 30초간 세 영상이 유지된다.

### 3단계 — 촬영 공간과 카메라 조정

위에서 본 초기 배치:

```text
              물체 ●
           ↗    ↑    ↖
       CAM1    CAM2    CAM3
       왼쪽    정면    오른쪽
       -45°     0°     +45°
```

1. 물체 중심과 놓는 방향을 테이프로 표시한다.
2. 카메라 높이를 물체 중심 높이 근처로 맞추고 수평으로 바라보게 한다. 세 카메라의 기울기·회전은 최대한 줄인다.
3. 비슷한 거리에서 시작해 물체 전체와 여백이 보이도록 조정한다. 물체 깊이가 촬영 거리보다 작을수록 직교투영 근사가 유리하다.
4. 각도를 바꾸며 시점 차이와 실루엣 품질을 확인한다. ±45°는 시작값이며 최적값 보장이 아니다.
5. 확정한 위치·각도·높이·거리를 기록하고 고정한다. 카메라를 이동하면 투영 설정도 다시 맞춘다.
6. 실제 물체 위치에서 카메라별 초점을 맞춘다. 세 화면 각각에서 가장자리가 선명한지 확인하며, 초점 설정 숫자를 서로 같게 맞출 필요는 없다. 고정 초점 카메라는 선명하게 보이는 거리로 조정하고 배치를 다시 기록한다.
7. 자동 초점이 계속 움직이면 지원되는 경우 초점을 맞춘 뒤 수동으로 고정한다. 설정을 지원하지 않으면 그 상태를 기록하고 촬영 직전 선명도를 확인한다. 물리적인 렌즈 조절이 필요한 장치는 사용자가 직접 조절한다.
8. 초점과 배치를 확정한 뒤 물체 중심에 세운 기준 막대처럼 공통 기준을 이용해 카메라별 영상 중심과 높이 배율을 맞춘다. 기준물을 제거한 뒤 촬영한다. 이후 초점·거리·줌을 바꾸면 영상 기준도 다시 확인한다.

**성공 기준:** 세 카메라 모두 물체 전체와 선명한 가장자리를 보여주고, 촬영 직전까지 초점이 안정적이며, 공통 중심과 수직 길이가 설정한 투영 기준에 대략 맞는다.

**초점과 체커보드의 차이:** 초점은 물체를 선명하게 보이게 하는 조절이다. 체커보드 보정은 알려진 격자 모양으로 카메라의 투영 특성·왜곡 등을 계산하는 작업이다. 이번 120분 데모에서는 초점을 확인하고 수동 투영 기준을 맞춘다. 체커보드 보정은 필수 단계에 추가하지 않고 8절의 후속 확장으로 둔다.

**주의:** 물체가 화면 중앙에 보이는 것만으로 카메라 보정이 끝나지는 않는다. 정면·좌우 대각선 영상을 앞·옆·위 영상처럼 취급하지 않는다.

### 4단계 — 실루엣과 잡음 제거

1. 원본을 회색조로 바꾼다.
2. 흰 배경·어두운 물체 조건에서 역이진화를 적용한다. 임계값 120은 시작값이며 카메라별로 조절한다.
3. 작은 커널부터 열기(opening)·닫기(closing)를 적용한다. 3×3부터 시작하고 필요할 때 늘린다.
4. 물체가 있는 관심 영역(ROI) 안에서 가장 큰 연결 성분을 남긴다. 원래의 구멍은 가능하면 유지한다.
5. 원본 옆에 마스크를 표시해 물체가 잘리거나 그림자가 붙는지 확인한다.

**성공 기준:** 마스크 값은 0/255이며 물체 윤곽이 안정적이고 화면 경계에 닿지 않는다.

**주의:** 검출 영역이 없을 때 `max(contours)`를 호출하지 않는다. 외곽선만 채우면 손잡이 구멍 등이 사라질 수 있다. 물체에 붙은 큰 그림자는 형태학 연산만으로 해결되지 않으므로 조명·배경부터 조정한다.

### 5단계 — 정지 물체 한 번 스캔

1. 물체와 카메라를 멈춘 상태에서 `S`를 누른다.
2. 유효한 원본 세 장과 해당 프레임으로 만든 마스크 세 장을 같은 촬영 폴더에 저장한다.
3. 장치 번호, 이미지 크기, 임계값, 카메라 각도, 중심·배율, 근사 방식, 촬영 시각을 `config.json`에 기록한다. 카메라별 초점 방식(고정/수동/자동), 확인 가능한 설정값, 선명도 확인 여부도 기록한다. 읽을 수 없는 설정값은 미확인으로 남긴다.
4. 저장 성공 여부를 확인한다. 다시 촬영하면 새 폴더를 만들어 이전 결과를 보존한다.

**성공 기준:** 저장된 파일을 다시 읽을 수 있고 원본과 마스크의 크기가 일치한다.

**주의:** 일반 USB 카메라 세 대를 순서대로 읽는 것은 하드웨어 동기 촬영이 아니다. 이번 프로젝트는 정지 물체만 다룬다.

### 6단계 — 3D 공간과 실루엣 교집합

마스크 세 장을 단순히 같은 픽셀 위치에서 AND 하면 3D가 되지 않는다. 각 3D 점이 각 카메라에서 어디에 보이는지 먼저 계산해야 한다.

**좌표 약속과 근사 투영 설계**

- 물체 중심을 원점으로 하고 X는 오른쪽, Y는 정면 카메라 쪽, Z는 위쪽으로 둔다.
- 각도 θ는 위에서 볼 때 +Y 방향에서 +X 방향으로 회전한 카메라 위치 각도이다.
- θ는 CAM1=-45°, CAM2=0°, CAM3=+45°로 시작하되 실제 배치값으로 수정한다.
- 카메라는 수평으로 원점을 바라보고 영상의 좌우 반전은 사용하지 않는다고 가정한다.
- 공간 범위는 우선 X,Y,Z 각각 [-1,1], 해상도는 축마다 40칸으로 둔다. 단위는 정규화 단위이다.

각 복셀 중심 `(X,Y,Z)`의 영상 위치:

```text
u = cx + s * (X*cos(θ) - Y*sin(θ))
v = cy - s * Z
```

`cx,cy`는 공통 원점이 보이는 영상 위치, `s`는 정규화 길이당 픽셀 수이며 카메라마다 따로 설정한다. 예를 들어 공통 기준 막대 높이를 정규화 길이 1로 정했다면 그 막대의 픽셀 높이가 s가 된다. 계산 시 각도를 라디안으로 변환한다.

1. 위 식으로 세 카메라에 투영한다.
2. 픽셀 좌표를 정수로 반올림하고 이미지 범위 안인지 먼저 검사한다.
3. 세 마스크에서 모두 흰색인 복셀만 남긴다. 마스크는 `mask[v, u]` 순서로 조회한다.
4. 범위 밖 점은 이번 데모에서 제거한다. 따라서 복원 영역과 물체가 모든 영상 안에 들어와야 한다.
5. 카메라 하나를 적용할 때마다 남은 복셀 수를 기록한다.

**검증:** 같은 투영식으로 만든 합성 상자 실루엣을 먼저 사용한다. 교집합을 추가할 때 복셀 수가 늘지 않아야 한다. 빈 마스크 하나면 결과가 비어야 한다. 흰 마스크 세 장이면 모든 카메라 영상 안으로 투영되는 복셀이 남아야 한다. 이후 실제 마스크를 적용한다.

**성공 기준:** 합성 검증을 통과하고 실제 물체 입력에서 형태가 남는다. 단순히 복셀 수가 0보다 크다는 이유만으로 성공 판정하지 않고 실루엣과 결과 모양을 비교한다.

**주의:** 카메라마다 물체 경계 상자를 독립적으로 같은 폭·높이로 늘리면 시점별 비율이 손상된다. 원본 좌표를 유지하고, 자르거나 축소한다면 중심·배율도 같은 변환으로 바꾼다.

### 7단계 — 표시와 재현

1. 남은 복셀을 Matplotlib의 3D 복셀 표시 기능으로 그린다.
2. X/Y/Z 축의 표시 비율을 동일하게 맞춘다.
3. `voxels.npy`와 `result.png`를 저장한다.
4. 저장된 촬영 폴더만으로 다시 복원한다.

**성공 기준:** 상자·원기둥의 대략적인 형태가 보이고, 같은 입력·설정으로 같은 복셀 배열을 얻는다.

### 8단계 — 시연과 완료 기록

- 세 영상 → 세 마스크 → 3D 결과 순서로 시연한다.
- 복원 방식, 실제/합성 입력 여부, 성공·실패한 기능을 기록한다.
- 뒷면 정보 부족, 내부 형상 누락, 수동 정렬과 원근 효과에 따른 오차를 설명한다.
- 발표 문장: “세 카메라에서 얻은 실루엣과 수동 투영 설정을 이용해 물체의 대략적인 외형을 복셀로 재구성했습니다.”

## 5. 구현할 폴더 구조

초기 제공물은 PROJECT_PLAN.md와 AGENTS.md 두 문서였다. 2026-09-15에 아래 Python 스크립트와 합성 검증을 구현했다. 실제 카메라 검증은 미완료이며 사용법과 현재 진행 상태는 `docs/사용법.md`, `docs/진행방법.md`를 참고한다. AGENTS_GUIDE.md는 현재 폴더에 없다.

```text
scanner-mini-project/
├─ PROJECT_PLAN.md          # 진행 계획
├─ AGENTS_GUIDE.md          # Codex 지침 작성법
├─ AGENTS.md                # 이 프로젝트용 Codex 지침 예시
├─ requirements.txt         # opencv-python, numpy, matplotlib
├─ src/
│  ├─ check_cameras.py       # 장치 확인
│  ├─ capture.py             # 미리보기·실루엣·S 저장·Q 종료
│  ├─ silhouette.py          # 마스크 처리 함수
│  ├─ reconstruct.py         # 투영·복셀 교집합·결과 저장
│  └─ visualize.py           # 저장된 복셀 표시
├─ tests/
│  └─ test_projection.py     # 합성 입력·경계 조건 검증
├─ captures/
│  └─ scan_001/
│     ├─ cam1.png / cam2.png / cam3.png
│     ├─ cam1_mask.png / cam2_mask.png / cam3_mask.png
│     └─ config.json
└─ outputs/
   └─ scan_001/
      ├─ voxels.npy
      └─ result.png
```

## 6. 실행 순서 — 구현 완료 후 사용할 명령 규격

아래 명령에 맞는 스크립트와 인자를 구현했다. Windows PowerShell에서 프로젝트 폴더를 연 다음 진행한다. 현재 기본 Python 경로가 깨져 있어 이 PC에서는 `.\.runtime\python\python.exe`를 사용한다. 자세한 명령은 `docs/사용법.md`에 기록했다.

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install opencv-python numpy matplotlib
.\.venv\Scripts\python.exe -m pip freeze > requirements.txt
.\.venv\Scripts\python.exe src/check_cameras.py
.\.venv\Scripts\python.exe src/capture.py --cameras 0 1 2 --output captures/scan_001
```

확인한 장치 번호로 0 1 2를 바꾼다. 미리보기에서 위치·초점·임계값·투영 기준을 순서대로 맞추고 S로 저장한 뒤 Q로 종료한다. 가상환경 Python을 직접 호출하므로 활성화 스크립트 실행은 필요 없다.

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests
.\.venv\Scripts\python.exe src/reconstruct.py --input captures/scan_001 --grid 40 --output outputs/scan_001
.\.venv\Scripts\python.exe src/visualize.py --input outputs/scan_001/voxels.npy
```

촬영 설정은 `captures/scan_001/config.json`에서 불러오도록 구현한다. 다음 스캔에서는 촬영·출력 경로를 `scan_002`처럼 바꾼다.

## 7. 막혔을 때와 시간 초과 대응

| 증상 | 우선 확인 | 일정 대응 |
|---|---|---|
| 한 카메라가 열리지 않음 | 번호, 다른 앱의 점유, 카메라 권한 | 장치를 하나씩 다시 확인 |
| 경계가 흐리거나 선명도가 계속 변함 | 물체 거리, 카메라별 초점, 자동 초점 반복 동작 | 초점·거리부터 조정하고 투영 기준 재확인 |
| 세 대 동시 연결에서 끊김 | 낮은 해상도·프레임률, USB 포트 분산 | 정상 저장한 입력으로 이후 알고리즘 작업 |
| 배경까지 흰 마스크가 됨 | 역이진화 방향, 조명, 임계값, ROI | 60분까지 마스크 품질에 집중 |
| 결과가 모두 사라짐 | 마스크 반전, 중심·배율, 각도 부호, 좌표 범위 | 카메라별 잔여 수·투영점 오버레이로 원인 확인 |
| 결과가 너무 크거나 길쭉함 | 각도, 영상 배율, 시점 부족 | 보정 한계를 기록하고 임의로 축을 찌그러뜨리지 않음 |
| 표시가 느림 | 그리드 크기 | 40에서 24로 낮추기 |

70분까지 마스크 저장이 안 되면 촬영·마스크를 최소 성과로 마무리한다. 95분까지 실제 복원이 실패하면 합성 입력 결과를 알고리즘 시연으로 제시하고 실제 복원은 미완료로 남긴다. 저장 입력 대체는 세 카메라 하드웨어 확인을 대신하지 않는다.

## 8. 주의사항과 다음 확장

- 체커보드 보정은 초점 조절을 대신하지 않는다. 확장할 때는 먼저 초점·줌·촬영 해상도를 확정하고 보정한다. 광학 설정을 바꾸면 기존 보정값의 유효성을 다시 확인한다.
- 카메라별 체커보드 촬영으로 내부 특성과 왜곡을 보정하는 작업에 더해, 세 카메라의 위치·방향을 같은 좌표계로 연결해야 한다. 서로 무관한 체커보드 자세에서 얻은 카메라별 결과를 그대로 공통 좌표계로 취급하지 않는다.
- 세 방향 실루엣만으로 모든 표면·깊이가 정해지지 않는다. 컵 내부나 가려진 오목면은 특히 어렵다.
- 세 카메라가 모두 정면 부근이면 뒷면 제약이 약하다. 후속 실험에서 더 넓은 방향 배치를 비교한다.
- 손잡이·가는 부분은 잡음 제거 과정에서 사라질 수 있다.
- 원근 효과·렌즈 왜곡·카메라 기울기는 이번 수동 근사에 남는 오차이다. 결과에 mm/cm 정확도를 주장하지 않는다.
- 정식 확장에서는 카메라별 내부 파라미터·왜곡을 구하고, 같은 세계 좌표계에 대한 외부 자세를 구한다. `cv2.projectPoints()`로 3D 점을 각 영상에 투영하며, 왜곡된 원본과 보정 영상 중 무엇을 쓰는지 일관되게 맞춘다. 자세를 구하는 기준판은 카메라 간 공통 좌표를 제공해야 한다. [OpenCV 카메라 보정 공식 문서](https://docs.opencv.org/4.13.0/dc/dbb/tutorial_py_calibration.html)

## 9. 완료 체크리스트

- [ ] 세 카메라 장치 번호와 동시 입력 확인
- [ ] 카메라·물체 위치 고정 및 설정 기록
- [ ] 카메라 3대 각각 초점·선명도 확인, 가능한 경우 초점 고정 및 상태 기록
- [ ] 세 실루엣 확인
- [ ] 원본·마스크·설정 한 묶음 저장
- [x] 합성 투영·빈 마스크·경계 조건 검증
- [ ] 실제 입력 복원 및 원본과 비교
- [x] 결과 이미지·복셀 저장과 재실행 (합성 입력 기준, 실제 입력은 미확인)
- [x] 구현 범위·실제 검증 여부·한계 기록

진행 기록: 완료 단계 / 소요 시간 / 발생 문제 / 다음 작업을 여기에 덧붙인다.

### 2026-09-15 구현 및 검증

- 카메라 검사, 세 영상/마스크 미리보기, 초점 설정 요청·기록, 새 촬영 폴더 저장, 수동 직교투영 복셀 복원, PNG/NPY 저장과 표시를 구현했다.
- Python 3.13.7을 프로젝트 `.runtime/python`에 준비했다. 기본 `python`은 없는 Python 3.14 실행 파일을 가리킨다.
- 자동 테스트 7개 통과. 합성 상자 40³ 복셀 수는 25,152 → 14,832 → 11,040이며 같은 저장 입력을 다시 복원한 배열이 완전히 일치했다.
- 합성 결과: `outputs/synthetic_box/result.png`, `voxels.npy`, `metadata.json`. 결과 이미지를 열어 축 표시와 근사 형태를 확인했다.
- 카메라 0~5 열기 실패. 실제 장치 대응·동시 미리보기·초점·마스크 품질·실제 복원은 미확인이다. GUI 표시의 실제 조작 역시 미확인이다.
- 사용법과 진행 방법: `docs/사용법.md`, `docs/진행방법.md`. 다음 작업은 카메라 연결·권한·장치 번호를 확인한 뒤 실제 촬영 검증이다.
- 소요 시간은 별도 측정하지 않았으며 120분 일정은 목표 시간표로 유지한다.
